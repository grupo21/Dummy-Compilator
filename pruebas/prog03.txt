program prog03 is
	{*Esta es la declaracion de variables*}
	val a, c fg: real; {*Aquí tiene que detectar error*}
	var f, g, h: integer;
	
procedure intercambiar(x, y :in out integer) is
	var auxiliar: integer;
	
	aux:=x;
	x:=y;
	y:=aux;
	
endprocedure intercambiar;

begin
	get(x); get(b);
	intercambiar(x,b); {*Que pasara aqui?*}
	put_line(x);
	put_line(b);
endprogram;
	
	